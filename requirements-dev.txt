# Dev-only dependencies — needed to execute / re-render notebooks/analysis.ipynb.
# Not required to run the analysis itself (that's requirements.txt).
#   pip install -r requirements.txt -r requirements-dev.txt

nbconvert>=7,<8
ipykernel>=6,<7
nbformat>=5,<6
